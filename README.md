```bash
sudo apt install wget -y && wget https://raw.githubusercontent.com/cheesecs/initserver/main/initserver.sh && sudo bash initserver.sh
```

```node
sudo apt install wget -y && wget https://raw.githubusercontent.com/cheesecs/initserver/main/initnodeserver.sh && sudo bash initnodeserver.sh
```
